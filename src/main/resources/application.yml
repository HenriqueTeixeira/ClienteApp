spring:
  application:
    name: ClienteApp

  rabbitmq:
    host: rabbitmq
    port: 5672
    username: guest
    password: guest

  # Configuração do log
  logging:
    level:
      root: INFO
      org.springframework.amqp: DEBUG

server:
  port: 8080